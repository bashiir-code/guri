# dependencies
node_modules/

# builds
dist/
.next/
out/
*.tsbuildinfo

# env — never commit real secrets (CLAUDE.md rule)
.env
.env.*
!.env.example

# serwist — generated service worker (built by `next build`)
apps/web/public/sw.js
apps/web/public/sw.js.map
apps/web/public/swe-worker*.js

# logs & misc
*.log
coverage/
.DS_Store

# design-sync — staged scripts, build output, machine state (previews/ and config ARE committed)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
apps/web/.ds-css/
apps/web/.ds-fonts/

# local Claude Code permissions (machine-specific)
.claude/settings.local.json
